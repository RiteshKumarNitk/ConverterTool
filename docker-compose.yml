services:
  backend:
    build: ./backend
    container_name: converter_backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: .
    container_name: converter_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
